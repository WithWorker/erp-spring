server:
  port: 7777
  tomcat:    
    additional-tld-skip-patterns: "*.jar"
spring:
  mybatis:
    mapper-locations: classpath:/mappers/*.xml  # MyBatis XML 파일 경로
    type-aliases-package: com.erp.backend.dto  # DTO 클래스 패키지 경로
  datasource:
    url: jdbc:mysql://localhost:3306/erp_db
    username: "root"
    password: "Kimjy0401!"
    driver-class-name: com.mysql.cj.jdbc.Driver
  output:
    ansi:
      enabled: always
  logging:
    level:
      org.mybatis: DEBUG
      org.mybatis.spring: DEBUG