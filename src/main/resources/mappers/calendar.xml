<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.mapper.ApprovalMapper">
	
  <!-- 일정 목록 -->
  <select id="calendarList" parameterType="java.util.Map" resultType="java.util.Map">
		SELECT 
      approval.*,
      'approval' AS event_type  
    FROM approval 
    UNION
    SELECT 
        calendar.*,
        'calendar' AS event_type  
    FROM calendar;
	</select>

  <!-- 일정등록 -->
  <insert id="calendarInsert" parameterType="com.erp.backend.model.Calendar">
		INSERT INTO calendar (title, content, start_date, end_date, applicant_id)
    VALUES (#{title}, #{content}, #{start_date}, #{end_date}, #{applicant_id}) 
  </insert> 

  <!-- 일정수정 -->
  <update id="calendarUpdate" parameterType="map">
    UPDATE calendar
    SET title = #{title}, 
        content = #{content}, 
        start_date = #{start_date}, 
        end_date = #{end_date}
    WHERE calendar_id = #{calendar_id}
  </update>

  <!-- 일정삭제 -->
  <delete id="calendarDelete" parameterType="map">
    DELETE FROM calendar WHERE calendar_id = #{calendar_id}
  </delete>

</mapper>